# TODO: Fix Special Link Features Implementation

## Issue 1: Missing `/api/share/verify` endpoint
- [x] Create `/app/api/share/verify/route.js` to verify JWT tokens

## Issue 2: Session bypass not read client-side  
- [x] Create `/app/api/session/route.js` to check if user has session bypass
- [x] Update client-side code to call session API

## Issue 3: Token parameter naming mismatch
- [x] Update `page.js` and `'use client'.js` to check for `k` parameter for Kindred access
- [x] Add sessionStorage check for kindred_access flag

## Issue 4: Share button not visible on property page
- [x] Add Share button to property page after user unlocks property

## Issue 5: Navigation to other properties should show lead form for shared links
- [x] Ensure share token only unlocks specific property, not globally

